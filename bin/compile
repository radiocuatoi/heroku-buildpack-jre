#!/bin/sh

echo "----> Welcome to JRE build pack"
echo "Extracting jre..."
BUILD_DIR=$1
tar -xf bin/jre.tar $BUILD_DIR/jre
ls $BUILD_DIR/jre

#Setting profile
PROFILE_PATH="$BUILD_DIR/.profile.d/java.sh"
mkdir -p $(dirname $PROFILE_PATH)
echo 'export PATH="$PATH:$HOME/jre/bin"' >> $PROFILE_PATH
